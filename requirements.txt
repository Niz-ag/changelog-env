# Core dependencies
openenv-core
fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.7.1
openenv
# For baseline inference
openai==1.30.1

# For GRPO training (Module 5)
# Install separately: pip install "trl>=0.17.0" transformers datasets accelerate vllm trackio torch
